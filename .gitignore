# Python
__pycache__/
*.pyc
*.pyo

# Checkpoints & large models (do NOT commit)
*.pth
*.pt

# Datasets (keep processed labels, ignore raw/heavy)
datasets/**/raw/
datasets/**/tmp/
datasets/**/cache/
# keep labels.csv if it exists
!datasets/**/labels.csv

# Runs & intermediates
runs/
!runs/eval/
!runs/eval/*.csv
!runs/eval/*.png
!runs/eval/error_sets/*.csv

# Notebooks & local env
.ipynb_checkpoints/
.env
.venv/
*.log

# IDE / OS junk
.vscode/
.DS_Store

# === ISHI Android signing (do not commit) ===
android/ishi-upload.jks
android/key.properties
*.keystore
*.jks

# === ISHI Android signing (do not commit) ===
android/ishi-upload.jks
android/key.properties
mobile/android/ishi-upload.jks
mobile/android/key.properties
*.keystore
*.jks


# derived crop set (regenerate via scripts/build_crops_manifest.py)
data/crops/
# local undo-net for normalize_masks.py (raw originals are in git history)
data/rescue_italy_1_31/
